name: CI of the Project

on:
  push:
    branches:
      - master

jobs:
  release:
    name: Test if it's working
    runs-on: ubuntu-latest

    steps:
      - name: hello
        run: echo "hello world!"
